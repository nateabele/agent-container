{
  "name": "agent-container",
  "version": "1.0.0",
  "description": "A Docker container environment for AI agents with headless browser automation capabilities",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node dist/tests/test-browser.js",
    "start": "npm run build && node dist/index.js",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "ai",
    "agent",
    "docker",
    "browser",
    "automation",
    "claude",
    "codex",
    "gemini",
    "cursor",
    "typescript"
  ],
  "author": "",
  "license": "MIT"
}
